# Instruções:
1. instalar dependencias
2. Criar arquivo `.env` com as variaveis `DB_ADMIN_USER` e `DB_PASSWORD`
3. Configurar o `.aws\credencials` antes
4. Rodar o arquivo `rds_manager.py` do diretorio principal